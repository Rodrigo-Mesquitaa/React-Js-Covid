// jest-dom adiciona matchers jest customizados para declaração em nós DOM.
// permite que você faça coisas como:
// expect (element) .toHaveTextContent (/ react / i)
import '@testing-library/jest-dom/extend-expect';
